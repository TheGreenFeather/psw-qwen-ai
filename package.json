{
  "name": "psw-qwen-ai",
  "version": "1.0.0",
  "description": "A Cloudflare Worker that runs the Qwen2.5-1.5B-Instruct model using onnxruntime-web and tokenizers-wasm.",
  "main": "worker.js",
  "scripts": {
    "build": "wrangler build worker.js",
    "deploy": "wrangler publish worker.js"
  },
  "dependencies": {
    "onnxruntime-web": "^1.20.1",
    "tokenizers": "^0.13.3"
  },
  "devDependencies": {
    "wrangler": "^2.0.0"
  },
  "license": "ISC"
}
